# Variables usadas por docker-compose.yml en la raíz del repo.
# Copia este archivo como .env y completa los valores reales antes de correr:
#   docker compose up --build

DB_ROOT_PASSWORD=cambiar_password_root
JWT_SECRET=cambiar_por_secreto_largo_y_aleatorio
MAILTRAP_HOST=smtp.mailtrap.io
MAILTRAP_PORT=587
MAILTRAP_USER=tu_usuario_mailtrap
MAILTRAP_PASS=tu_password_mailtrap
MAIL_FROM=noreply@tudominio.local
