# Ruta relativa: el proxy de Vite (server.proxy en vite.config.js) reenvia
# /api/cajas a Express y el resto de /api a Laravel. No pongas aqui la URL
# publica de Codespaces - eso rompe CORS (ver historial del proyecto).
VITE_API_URL=/api
