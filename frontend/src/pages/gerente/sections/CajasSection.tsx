import type { Caja } from '../types'

interface CajasSectionProps { cajas: Caja[]; onCreate: () => void; onEdit: (caja: Caja) => void; onToggle: (id: number, estado: string) => void }

function CajasSection({ cajas, onCreate, onEdit, onToggle }: CajasSectionProps) {
  return <section className="mt-6 rounded-2xl border border-[var(--borde)] bg-[var(--bg-card)] px-7 py-6 shadow-[var(--sombra)]"><div className="mb-5 flex items-center justify-between"><h3 className="font-['Syne'] text-[1rem] font-bold text-[var(--texto)]">Gestión de Cajas</h3><button className="rounded-[10px] bg-[var(--rojo)] px-4 py-3 font-['DM_Sans'] text-[0.875rem] font-semibold text-white" onClick={onCreate}>+ Nueva Caja</button></div><div className="overflow-x-auto"><table className="w-full border-collapse"><thead><tr>{['Nombre', 'Estado', 'Acciones'].map(titulo => <th key={titulo} className="border-b border-[var(--borde)] px-3 py-2 text-left text-[0.78rem] font-semibold text-[var(--texto-muted)]">{titulo}</th>)}</tr></thead><tbody>{cajas.length === 0 ? <tr><td colSpan={3} className="py-4 text-center text-[var(--texto-muted)]">Cargando...</td></tr> : cajas.map(caja => <tr key={caja.id_caja}><td className="border-b border-[var(--borde)] px-3 py-2.5 text-[0.85rem] text-[var(--texto)]"><strong>{caja.nombre}</strong></td><td className={`border-b border-[var(--borde)] px-3 py-2.5 text-[0.85rem] ${caja.estado === 'ACTIVA' ? 'text-[var(--verde)]' : 'text-[var(--rojo)]'}`}>{caja.estado}</td><td className="border-b border-[var(--borde)] px-3 py-2.5"><div className="flex gap-2"><button className="rounded-[10px] border border-[var(--borde)] bg-[#f9f5f0] px-3 py-1.5 text-[0.8rem] font-semibold text-[var(--texto-muted)]" onClick={() => onEdit(caja)}>Editar</button><button className="rounded-[10px] px-3 py-1.5 text-[0.8rem] font-semibold text-[var(--texto-muted)]" onClick={() => onToggle(caja.id_caja, caja.estado)}>{caja.estado === 'ACTIVA' ? 'Desactivar' : 'Activar'}</button></div></td></tr>)}</tbody></table></div></section>
}

export default CajasSection