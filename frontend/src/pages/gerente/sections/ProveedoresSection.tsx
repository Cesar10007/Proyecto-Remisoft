import type { Proveedor } from '../types'

interface ProveedoresSectionProps { proveedores: Proveedor[]; onCreate: () => void; onEdit: (proveedor: Proveedor) => void; onToggle: (id: number, estado: string) => void }

function ProveedoresSection({ proveedores, onCreate, onEdit, onToggle }: ProveedoresSectionProps) {
  return <section className="mt-6 rounded-2xl border border-[var(--borde)] bg-[var(--bg-card)] px-7 py-6 shadow-[var(--sombra)]"><div className="mb-5 flex items-center justify-between"><h3 className="font-['Syne'] text-[1rem] font-bold text-[var(--texto)]">Gestión de Proveedores</h3><button className="rounded-[10px] bg-[var(--rojo)] px-4 py-3 font-['DM_Sans'] text-[0.875rem] font-semibold text-white" onClick={onCreate}>+ Nuevo Proveedor</button></div><div className="overflow-x-auto"><table className="w-full border-collapse"><thead><tr>{['Nombre', 'Contacto', 'Teléfono', 'Email', 'Tipo', 'Estado', 'Acciones'].map(titulo => <th key={titulo} className="border-b border-[var(--borde)] px-3 py-2 text-left text-[0.78rem] font-semibold text-[var(--texto-muted)]">{titulo}</th>)}</tr></thead><tbody>{proveedores.length === 0 ? <tr><td colSpan={7} className="py-4 text-center text-[var(--texto-muted)]">Cargando...</td></tr> : proveedores.map(proveedor => <tr key={proveedor.id_proveedor}><td className="border-b border-[var(--borde)] px-3 py-2.5 text-[0.85rem] text-[var(--texto)]"><strong>{proveedor.nombre}</strong></td><td className="border-b border-[var(--borde)] px-3 py-2.5 text-[0.85rem] text-[var(--texto)]">{proveedor.nombre_contacto}</td><td className="border-b border-[var(--borde)] px-3 py-2.5 text-[0.85rem] text-[var(--texto)]">{proveedor.telefono}</td><td className="border-b border-[var(--borde)] px-3 py-2.5 text-[0.85rem] text-[var(--texto)]">{proveedor.email}</td><td className="border-b border-[var(--borde)] px-3 py-2.5 text-[0.85rem] text-[var(--texto)]">{proveedor.tipo_proveedor}</td><td className={`border-b border-[var(--borde)] px-3 py-2.5 text-[0.85rem] ${proveedor.estado === 'ACTIVO' ? 'text-[var(--verde)]' : 'text-[var(--rojo)]'}`}>{proveedor.estado}</td><td className="border-b border-[var(--borde)] px-3 py-2.5"><div className="flex gap-2"><button className="rounded-[10px] border border-[var(--borde)] bg-[#f9f5f0] px-3 py-1.5 text-[0.8rem] font-semibold text-[var(--texto-muted)]" onClick={() => onEdit(proveedor)}>Editar</button><button className="rounded-[10px] px-3 py-1.5 text-[0.8rem] font-semibold text-[var(--texto-muted)]" onClick={() => onToggle(proveedor.id_proveedor, proveedor.estado)}>{proveedor.estado === 'ACTIVO' ? 'Desactivar' : 'Activar'}</button></div></td></tr>)}</tbody></table></div></section>
}

export default ProveedoresSection